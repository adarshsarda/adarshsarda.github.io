---
// Multimodale Emotionserkennung wird als Publikation gefuehrt, nicht als Fallstudie.
// Leitet die alte Projekt-URL auf die Publikationsseite um.
const destination = '/de/publications/';
return Astro.redirect(destination, 301);
---
