---
// Multimodal emotion recognition is presented as a publication, not a case file.
// Redirect the old project URL to the Publications page.
const destination = '/publications/';
return Astro.redirect(destination, 301);
---
