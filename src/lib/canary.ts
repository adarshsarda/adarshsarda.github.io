/**
 * An inert canary string. It is a nod to the backdoor / evaluation work, not a
 * live detector. Kept in one place so the footer marker and the terminal's
 * `canary` command can never drift apart.
 */
export const CANARY = 'CANARY-98A3F-ODSB-2026';
