# build output
dist/

# generated Astro types / cache
.astro/

# dependencies
node_modules/

# logs & local env
*.log
npm-debug.log*
.env
.env.*
!.env.example

# os cruft
.DS_Store
Thumbs.db

# local source bundles
*.zip

# local agent / editor config
.claude/
.obsidian/

# scratch
tmp/

# ===== Private wiki lane (local-only; never committed to this public repo) =====
# Personal knowledge base: raw sources, offensive tradecraft, unpublished
# research directions, and mirrored external references. These stay on the
# local machine and are NOT part of the published site or the public repo.
# They are backed up only by the separate private overlay when that overlay is
# explicitly synchronized; public Git never protects them.
private/
content/redteam/
content/project-ideas/
content/literature/
content/profile/positioning.md

# agent constitution — kept out of the public repo, tracked in the private repo
AGENTS.md
